version: 2.1

jobs:
  build_and_test:
    docker:
      - image: circleci/python:2.7
      - image: rabbitmq:3
      - image: mongo:3.4

    steps:
      - checkout
      - run:
          name: Run tests
          command: make all

workflows:
  build_test_deploy:
    jobs:
      - build_and_test
